apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-gateway
  namespace: llm-inference
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: gateway
          imagePullPolicy: Never
