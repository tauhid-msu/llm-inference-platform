apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: llm-inference
  labels:
    app.kubernetes.io/name: llm-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llm-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
